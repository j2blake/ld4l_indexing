loc_lang.rdf is from somewhere. It allows us to get the English labels for language codes.

topic_labels.nt is a concatenation of all of the other files (with no extension).
It allows us to get the English labels for topics.
